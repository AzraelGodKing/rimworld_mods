{
  "name": "RimWorld Mods",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "docs-site",
      "command": "python3 -m http.server 8000 --bind 0.0.0.0 --directory docs",
      "description": "Serves the static GitHub Pages site (docs/) at http://localhost:8000 for local preview."
    }
  ],
  "ports": [
    {
      "name": "docs site",
      "port": 8000
    }
  ]
}
