# Languages

English keyed strings ship in `English/Keyed/ShiftChange.xml`.

To add a language, copy that file under `Languages/<LanguageName>/Keyed/ShiftChange.xml`, translate values, keep tag names and `{0}` placeholders.
